import { RouterConfig } from "../router";
import { StoreConfig } from "../store";

export type Config = {
  router: RouterConfig;
  store: StoreConfig;
  mixins: null;
  directives: null;
  filters: null;
  components: null;
};
